[Unit]
Description=24HG Forge Challenge Tracker — auto-track weekly challenge progress
After=graphical-session.target
Wants=network-online.target

[Service]
Type=oneshot
ExecStart=/usr/bin/bash -c '/usr/bin/forge-challenges daemon --once'
TimeoutStartSec=120
Environment=HOME=%h

[Install]
WantedBy=default.target
