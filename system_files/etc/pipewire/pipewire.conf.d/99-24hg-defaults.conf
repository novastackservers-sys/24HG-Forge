# 24HG Default PipeWire Configuration
# Balanced settings for gaming + desktop use.
# Switch to low-latency with: 24hg-audio gaming
context.properties = {
    default.clock.quantum = 512
    default.clock.min-quantum = 128
    default.clock.max-quantum = 2048
    default.clock.rate = 48000
}
